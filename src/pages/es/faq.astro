---
import SiteLayout from '../../layouts/SiteLayout.astro';
import FAQBlock from '@/components/blocks/faqs-2.astro';
import { getFaq } from '@/lib/content';
import { stripMarkdown } from '@/lib/markdown';

const items = (await getFaq('es')).map((q) => ({
  title: (q.frontmatter.question as string) ?? q.slug,
  description: stripMarkdown(q.body || ''),
}));
---

<SiteLayout lang="es" title={`Preguntas frecuentes – Personal Shopper`} description={'Respuestas a preguntas frecuentes'}>
  <FAQBlock items={items}>
    <h1>FAQ</h1>
    <p>Respuestas a preguntas frecuentes.</p>
  </FAQBlock>
</SiteLayout>
