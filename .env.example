# GitHub OAuth for Decap CMS
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional: scope override (default is "repo,user:email").
# For public repos you can use: public_repo
# GITHUB_OAUTH_SCOPE=public_repo

# Resend (email provider)
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=

# Email addresses
# From must be a verified sender/domain in Resend (e.g., "Personal Shopper <contact@your-domain.com>")
EMAIL_FROM=
# One or more comma-separated recipients (e.g., owner@example.com,assistant@example.com)
EMAIL_TO=
# Optional: comma-separated BCC recipients
# EMAIL_BCC=
